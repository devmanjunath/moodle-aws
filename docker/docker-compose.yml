version: "3.7"

networks:
  backend:

services:
  mysql_db:
    image: mysql
    container_name: moodle_db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: moodle
      MYSQL_USER: user
      MYSQL_PASSWORD: root
  moodle:
    depends_on: [mysql_db]
    build:
      context: ./moodle
    ports:
      - "443:443"
      - "80:80"
    command: >
      /bin/bash -c "/opt/entrypoint.sh --host-name 'localhost' --db-type "mysqli" --db-host mysql_db --db-name moodle --db-user 'user' --db-pass 'root' --site-name 'Test Drive' --admin-user 'admin' --admin-pass 'admin123'"
